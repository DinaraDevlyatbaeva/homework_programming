#include <stio.h>   //подключение функции стандартного ввода и вывода
int main (void) {   //все основные программы называются именем main
              int x;   //x целое число
              x = -3 * 4 % - 6 + 5;    // вычисление x
			  printf ("%d", x)     /* вывод значения x
			                            на экран */
		      return 0:   // выход из функции
}
